import { createSlice } from "@reduxjs/toolkit";

const unverifiedApiDataPacks=createSlice(
    {
        name:'verifedapipack',
        initialState:{unveridedPackInfo:['Teach your own Studenst\'s Via Invite','Use all the Features for your class'],
        
    
    },
        reducers:{

        }
    }
)
 export default unverifiedApiDataPacks.reducer